{
  "buildCommand": "rm -rf .angular && npm run build",
  "outputDirectory": "dist/quiz-dashboard/browser",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
